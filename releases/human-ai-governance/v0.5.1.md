# human-ai-governance v0.5.1

Date: 2026-08-05

Status: validated canonical release

## Scope

- Preserve the five-tier governance model and existing stage-sizing behavior.
- Repair staged-index and working-tree inspection in the generic preflight scaffold.
- Preserve exact Unicode, whitespace, rename, and trailing-space Git paths.
- Fail explicitly when Git state cannot be inspected.
- Make graph-manifest presence insufficient by itself to activate graph workflow.

## Deterministic Evidence

- Package regression: passed.
- Preflight behavior regression: passed, including staged/worktree divergence, Unicode and whitespace paths, rename source classification, marker and plan drift, `.env` deletion, Tier 1–5 behavior, and risk-reducing Tier 5 paths.
- Official Codex skill validator: passed.
- Installed package and the original v0.5.1 candidate were byte-equivalent before this repository import.

## Model Behavior Evidence

Model: `gpt-5.6-sol`

Reasoning efforts: `high`, `xhigh`

- Eight targeted A/B runs completed successfully with zero fixture changes.
- A bounded task in a graph-enabled repository did not read graph-governance files.
- A stale-graph task treated the graph as derived, preferred newer owner evidence, retained verified upstream state, blocked invalid continuation, and did not retry or rebuild.
- The candidate split the same required stale-graph evidence across one or two additional read-only commands in the single sample. This was recorded as run variance; it was not treated as proof of token improvement or regression.

## Release Boundary

This release changes the reusable skill and generic scaffold only. It does not migrate downstream project markers, copied project preflights, account permissions, deployments, or trading capabilities.
