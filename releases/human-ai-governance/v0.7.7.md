# human-ai-governance v0.7.7

Date: 2026-09-05

Status: validated local candidate

## Change

The user approved carrying existing agreement and authorization through later workflow steps. Governance guidance and its generated project template now ask again only for an action outside the accepted envelope, an unresolved material choice, or an unsatisfied approval or renewal condition. Existing expiry, one-shot, cumulative-limit, and platform boundaries remain effective.

This version includes the v0.7.6 graph-status and active-task-continuity changes. Reasoning-mode policy and generic preflight logic are unchanged; the preflight script changes only its version marker.

## Focused Evidence

An independent GPT-6 Astra decision exercise with requested effort `xhigh` read the current runtime instructions and synthetic task facts without the intended answers, diff, tests, or release records. For an already-approved non-force push with supplied current passing evidence, it proposed proceeding without another approval and verifying the result before reporting success. For a consumed one-shot paid-run authorization, it retained local diagnosis and preparation while preserving the next paid-execution gate.

These two decisions met the scoped expectations. No real external action was performed, and no long-horizon, cross-model, latency, or token-efficiency gain is claimed.

## Validation

- `.venv/bin/python scripts/manage_skills.py validate`: passed for three skills and nine regression scripts.
- Official Codex `quick_validate.py skills/human-ai-governance`: passed.
- `.venv/bin/python scripts/manage_skills.py check-all`: all three installation links point to their canonical source.
- `git diff --check` and scoped diff review: passed.

## Project Migration

After synchronizing the affected project rules, use:

```text
Generated/adapted from human-ai-governance v0.7.7
```

Preserve stronger project-specific controls. Copied templates should retain existing authorization and qualify renewed approval by the action's actual envelope and conditions. Updating the marker alone does not migrate those rules. Existing projects are not modified by this repository change.

## Local Boundary

Branch: `codex/astra-skill-adaptation`. Canonical source, tests, catalog, and version records only. No commit, push, tag, hosted release, installation-link mutation, global-rule edit, or downstream migration is included.
